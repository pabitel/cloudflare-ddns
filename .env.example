# Cloudflare API Token (从 Cloudflare 仪表板获取)
CF_TOKEN=your_cloudflare_api_token_here

# Zone ID (从 Cloudflare 域名概览页面获取)
ZONE_ID=your_zone_id_here

# 域名设置
DOMAIN=example.com
SUBDOMAIN=ddns

# Cron 定时表达式 (默认每5分钟更新一次)
CRON_SCHEDULE="*/5 * * * *"